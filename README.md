# 3logiya

Минималистичный и гибкий логгер на C++ с чистым и удобным API.

3logiya — это попытка сделать логирование простым, понятным и удобным в реальных проектах.

Без лишней магии. Просто берёшь и используешь.

---

## 📄 Содержание

1. [Философия проекта](#-философия-проекта)
2. [Возможности](#-возможности)
3. [Быстрый старт](#-быстрый-старт)
4. [Форматирование сообщений](#-форматирование-сообщений)
5. [Пример вывода](#-пример-вывода)
6. [Глобальный и локальный логгер](#-глобальный-и-локальный-логгер)
7. [Уровни логирования](#-уровни-логирования)
8. [Фильтрация логов](#-фильтрация-логов)
9. [Структура проекта](#-структура-проекта)
10. [Сборка](#-сборка)
11. [Почему не использовать другие логгеры](#-почему-не-использовать-другие-логгеры)
12. [Планы](#-планы)

---

## 🧠 Философия проекта

* Логгер должен быть удобным в использовании
* Архитектура должна быть понятной и прозрачной
* Форматирование отделено от вывода
* Стратегии вывода легко расширяются (Sink pattern)

Проект изначально создавался как инструмент для собственных проектов, но с прицелом на чистую архитектуру и удобный API.

---

## ✨ Возможности

* Удобная конфигурация через fluent API
* Несколько способов вывода (консоль, файл)
* Цветной вывод в консоль
* Поддержка DEBUG-логов (файл, строка, функция)
* Простая фильтрация по уровню логирования
* Минимум зависимостей

---

## 🚀 Быстрый старт

```cpp
#include "logger/logger.hpp"

int main() {
    auto& logger = Logger::create()
        .to_console()
        .to_file("app.log");

    logger.info("Hello, world!");
    logger.error("Something went wrong");

    LOG_INFO("Macro works too");
    LOG_DEBUG("Debug info");

    return 0;
}
```

---

## 📝 Форматирование сообщений

Методы `info`, `error` и `debug` поддерживают форматирование через `{}` и список аргументов:

```cpp
auto& logger = Logger::create()
    .to_console()
    .to_file("app.log");

int value = 42;

logger.info("User logged in: {}", "Alice");
logger.error("Failed to load resource: {}", "config.json");
logger.debug("Value of x = {}", value);
```

Макросы также поддерживают форматирование:

```cpp
LOG_INFO("User {} has {} points", "Bob", 120);
LOG_ERROR("Could not open file: {}", "data.txt");
LOG_DEBUG("Processing item {}", 7);
```

---

## 🎨 Пример вывода

```txt
[2026-03-17 02:02:46] [INFO ] Hello
[2026-03-17 02:02:46] [ERROR] Something failed
[2026-03-17 02:02:46] [DEBUG] main.cpp:12 main -> value = 42
```

---

## 🌐 Глобальный и локальный логгер

3logiya поддерживает два режима использования:

### 1️⃣ Глобальный логгер

* Создаётся через `Logger::create()`.
* Сразу становится **активным** и доступным для макросов `LOG_INFO`, `LOG_ERROR`, `LOG_DEBUG`.
* Удобно, когда нужен единый логгер на весь проект.

```cpp
auto& logger = Logger::create()
    .to_console()
    .to_file("app.log");

LOG_INFO("Hello from global logger");
```

📌 Макросы автоматически используют активный логгер — не нужно передавать его в каждый вызов.

### 2️⃣ Локальный логгер

* Создаётся как обычный объект `Logger`.
* Можно конфигурировать независимо от глобального логгера.
* Идеально для **отдельных модулей или тестов**, где нужен свой вывод или фильтр.

```cpp
Logger local_logger;
local_logger.to_console();
local_logger.info("Hello from local logger");
```

⚠️ Локальный логгер не влияет на макросы и глобальный логгер.

💡 **Вывод:**

* Используй **глобальный логгер** для быстрого проекта и макросов.
* Используй **локальные логгеры**, если нужны изолированные потоки логирования или разные фильтры/файлы.

---

## 🧾 Уровни логирования

```cpp
LogLevel::INFO
LogLevel::ERROR
LogLevel::DEBUG
```

DEBUG-логи игнорируются в release-сборке (`NDEBUG`).

---

## 🔍 Фильтрация логов

3logiya поддерживает фильтры на двух уровнях: на уровне логгера и на уровне отдельного sink’а. Это позволяет гибко управлять тем, какие сообщения и куда выводятся.

### 1️⃣ Фильтр логгера

Фильтр логгера ограничивает **весь поток логов**. Если установлен, логгер будет передавать события только указанного уровня во все подключённые sink’ы.

```cpp
auto& logger = Logger::create()
    .to_console()
    .to_file("logs.txt")
    .filter(LogLevel::ERROR); // логируем только ошибки

LOG_INFO("Это сообщение не появится");
LOG_ERROR("Только это будет записано");
```

### 2️⃣ Фильтр на уровне sink

Каждый sink может иметь свой собственный фильтр, что позволяет направлять разные уровни логов в разные места. Например, DEBUG в консоль, INFO в файл:

```cpp
auto& logger = Logger::create()
    .to_console(LogLevel::DEBUG)  // консоль видит DEBUG
    .to_file("app.log", LogLevel::INFO); // файл видит INFO

LOG_DEBUG("Консоль увидит это");  
LOG_INFO("Файл увидит это");
```

💡 **Итог:**

* Логгер фильтрует **весь поток** сообщений.
* Синк фильтрует **только свои записи**.
* Комбинируя их, можно гибко управлять тем, что и куда выводится.

---

## 🧩 Структура проекта

```
include/logger
    logger.hpp            - сам логгер
    log_event.hpp         - структура события
    log_level.hpp         - уровни логирования
    formatter/
        formatter.hpp     - форматирование

include/logger/sink
    sink.hpp              - базовый интерфейс
    console_sink.hpp      - вывод в консоль
    file_sink.hpp         - вывод в файл

src/
    реализация

example/
    пример использования
```

---

## 🛠️ Сборка

```bash
mkdir build
cd build
cmake ..
make
```

---

## ❓ Почему не использовать другие логгеры?

Этот проект сделан для:

* обучения
* полного контроля над поведением
* простоты интеграции

Без тяжёлых зависимостей.
Без сложной конфигурации.

---

## 🚧 Планы

* [X] Фильтрация на уровне отдельных sink’ов
* [ ] Асинхронное логирование
* [ ] Кастомные шаблоны форматирования
* [X] Потокобезопасность

---

## 💬 Заметка

Сделано ночью, на кофеине и инженерном упрямстве.
